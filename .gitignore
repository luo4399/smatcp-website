# ---------- macOS ----------
.DS_Store
._*
.Spotlight-V100
.Trashes

# ---------- Python ----------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ---------- 本地编辑备份（*.bak-YYYYmmdd_HHMMSS） ----------
*.bak-*
*.orig

# ---------- SomaCard 后端运行时产物 ----------
somacard/tmp/

# ---------- 编辑器 / 系统 ----------
*.swp
*.swo

# ---------- AI 工作区（绝不能入库） ----------
# 里面有 .workbuddy-ai/memory/，记录了服务器路径、内网地址与内部笔记。
# 一旦提交并推到远端就等于公开，务必保持忽略。
.workbuddy-ai/

# ---------- 设计稿投放目录 ----------
# Figma 导出的 PNG 体积大且属于过程稿，默认不入库；
# 若要连同设计稿一起归档，删掉下面这行即可。
design/*.png
design/*.jpg

# ---------- 构建产物：data/*.js（必须入库！）----------
# 这三个文件由 scripts/ 下的脚本从 data_source/ 的源表重建，
#   data/variants_data.js  ← scripts/build_variants_data.py
#   data/genome_data.js    ← scripts/build_genome_data.py
#   data/plots_index.js    ← scripts/build_plots_index.py
#
# 曾经把它们当「生成物」忽略掉，结果部署直接坏掉：Render 那条链
# （Procfile → gunicorn server.server:app）是**从 git 拉代码**的，
# clone 出来根本没有 data/ 目录，首页搜索表和 Data Portal 全都没数据。
# 更坑的是 /data/variants_data.js 会命中 serve_static 的回落逻辑，
# 返回 200 但内容是 index.html —— 只看状态码发现不了。
#
# 所以：**不要忽略它们**。改了源表就重新生成并一起提交。
# 代价是每次数据更新都会往 git 历史里加一个 8.3 MB 的 blob。
# 自查方式：git clone 到临时目录，跑 check_refs.py，应为 0 断链。

# ---------- 发布用快照目录 ----------
# publish/ 是给「发布为应用」用的纯净副本：只放站点公开文件，
# 不含 .workbuddy-ai/、README.md、somacard/ 后端、废弃数据等。
# 它是生成物，不入库；重建命令见 README「发布为公开链接」一节。
publish/
